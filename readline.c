#include "readline.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

char *readline(char *print) {
    int n;
    char *output = malloc(1000 * sizeof(char));
    while (output )


    return output;
}